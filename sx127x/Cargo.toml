[package]
name = "sx127x"
version = "0.1.0"
edition = "2021"

[features]
default = []
std = []

[dependencies]
embedded-hal = "1.0"
heapless = "0.8"
sx126x = { path = "../sx126x" }

[dev-dependencies]
embedded-hal-mock = { version = "0.10", features = ["eh1"] }
