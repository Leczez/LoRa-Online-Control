[package]
name = "sx126x"
version = "0.1.0"
edition = "2021"

[features]
default = []
std = []

[dependencies]
embedded-hal = "1.0"
embedded-io = "0.6"
heapless = "0.8"

[dev-dependencies]
embedded-hal-mock = { version = "0.10", features = ["eh1"] }
